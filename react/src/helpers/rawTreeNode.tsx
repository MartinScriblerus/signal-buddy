import { TreeNode } from '../components/CreateChuck';

const rawTree: TreeNode = {
    name: 'T',
    children: [
        {
            name: 'Inst_1',
            children: [],
        },
    ],
};
export default rawTree;